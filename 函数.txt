函数返回不能使局部对象的引用和指针，应为局部变量会被中途销毁

如果同一作用域内函数的名字相同但是形参列表不同，我们称它为重载函数

我们可以为一个或者多个形参提供默认实参，但是一旦某个形参被赋予默认实参，后面的形参都必须含有默认实参

inline 内联函数的用处，一般来说内联函数只是支持小的函数

constexpr 是c+11中新增的关键字，其语义是常量表达式，也就是在编译期可求值的表达式。返回都是字面类型。

内联函数和constexpr一般放在头文件中

assert()预处理宏， assert(expr)如果为假，assert就终止程序

